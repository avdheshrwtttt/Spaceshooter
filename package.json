{
  "name": "nebula-strike",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Nebula Strike — a premium browser space shooter built with TypeScript + Canvas.",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "vite": "^5.4.2"
  }
}
